import { Component } from '@angular/core';
import { NavController } from 'ionic-angular';

@Component({
  selector: 'page-expedition',
  templateUrl: 'expedition.html'
})
export class ExpeditionPage {

  constructor(public navCtrl: NavController) {
  }
  
}
